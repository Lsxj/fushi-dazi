# 辅食搭子产品需求文档（PRD）

> 文档版本：v0.17-draft
> 更新日期：2026-08-05
> 产品阶段：内测版，核心闭环已实现，进入发布前核验与测试收口  
> 适用范围：微信小程序；Contract API、React Solution Console 与 MCP Server 为开发者作品集和方案验证能力，不属于家长端正式交付范围

## 1. 产品概述

辅食搭子是面向 4–24 月龄宝宝照护者的辅食运营工具。它不是单纯的食谱库，而是根据宝宝月龄、排敏状态、身体状态、历史饮食和家庭库存，帮助家长完成“吃什么—怎么准备—实际吃了什么—是否有反应—下一步怎么调整”的日常决策闭环。

### 1.1 核心价值

- 降低每天决定宝宝吃什么的认知负担。
- 把排敏、安全规则和身体状态约束前置到推荐链路。
- 将菜单、库存、打卡、反应记录和复盘串成一个连续流程。
- AI 只负责理解问题和解释结果，食物安全判断由确定性规则执行。

### 1.2 目标用户

- 处于辅食添加阶段、需要高频安排菜单的新手家长。
- 需要多人协作照护、希望保留饮食与反应记录的家庭。
- 正在进行新食材排敏，容易遗漏观察窗口和重试时间的照护者。

### 1.3 非目标

- 不提供疾病诊断、过敏确诊或替代医生的医疗建议。
- 不做电商购买、社区内容、达人食谱或广告推荐。
- 当前版本不支持多宝宝、多照护者实时同步。

### 1.4 AI 工程作品集目标

- 用同一个真实业务问题展示“用户体验—AI 编排—安全边界—质量交付”的完整解决方案。
- 通过共享 Zod/oRPC 合约证明前后端类型安全与 contract-first API 设计能力。
- 通过 MCP、Skills 和 provider adapter 展示可组合、可审计的 agentic workflow。
- 在没有模型密钥、云部署或付费 GitHub 能力时，仍能在本地完整演示确定性规则、API、Web 交互与自动化测试。
- 所有作品集界面必须清楚区分真实业务能力、mock 状态与规划能力，避免用静态页面冒充已实现功能。

### 1.5 产品载体与边界

| 载体 | 面向对象 | 代码位置 | 当前定位与数据边界 |
|---|---|---|---|
| 微信小程序 | 宝宝家长与实际照护者 | 根目录 `app.ts`、`pages/` | 家长端产品；菜单、库存、打卡、排敏和反应记录主要使用 `wx.storage`，AI 会话按披露规则同步部分数据到 cloudDB |
| React Solution Console | 面试官、招聘方与技术评审 | `apps/web-console/` | 独立 Web 作品集，不是小程序页面，也没有嵌入小程序；用于演示安全规则、可观测性和家庭协作原型 |
| Contract-first API | Web 控制台与方案验证调用方 | `apps/api-server/` | 本地演示 API；家庭协作合成状态以原子文件写入支持重启恢复，并有乐观并发保护；不是生产云数据库 |
| MCP Server | AI Agent 与技术集成演示 | `mcp-server/` | 独立的工具、资源和提示词接口；测试数据与微信小程序用户数据隔离 |

当前 `http://127.0.0.1:4175/collaboration` 属于 React Solution Console。其家庭角色、申请确认、审计记录和菜单替换使用明确标注的 `synthetic-demo` 数据，不能视为微信小程序已经上线多人协作能力。

## 2. 当前进度摘要

截至 2026-08-05，家长端已具备完整的本地使用闭环，当前判断为“功能型 MVP 基本完成，发布条件尚未满足”。开发者作品集已形成 MCP Server、Contract-first API 和独立 React Solution Console 三个互相连接的演示面；其中 React 家庭协作原型尚未进入微信小程序。作品集 P0 已增加本地重启恢复与档案版本冲突保护，但生产级身份、云数据库和多副本一致性仍未实现。剩余家长端发布风险主要集中在真机 AI 链路和正式隐私/删除机制。

| 模块 | 状态 | 当前实现 |
|---|---|---|
| 首次建档 | ✅ 已完成 | 欢迎流程、宝宝姓名/生日/每日餐数、已吃食材、冰箱初始化 |
| 今日决策 | ✅ 已完成 | 今日菜单、下一步建议、身体状态、明日预处理、单餐替换 |
| 饮食执行 | ✅ 已完成 | 按计划打卡、实际餐修改、手动补记、偏好/份量、撤销打卡、自动扣减与恢复库存 |
| 周计划 | ✅ 已完成 | 计划生成、单餐替换、整体重排、采购清单、计划预览与复制 |
| 冰箱 | ✅ 已完成 | 添加、搜索、筛选、排序、份数、临期/低库存、批量处理、加入明日/采购清单 |
| 排敏档案 | ✅ 已完成 | 品类状态、单食材确认/例外、自定义食材、尝试/观察/过敏状态流转 |
| 反应记录 | ✅ 已完成 | 症状与严重度、72 小时回溯、可疑食材、处置、编辑/消退/删除 |
| 饮食复盘 | ✅ 已完成 | 时间流、筛选/搜索、7 日摘要、补记/编辑、文本导出 |
| 食谱库 | ✅ 已完成 | 127 道食谱，搜索、分类筛选、食谱详情 |
| AI 搭子 | 🟡 已实现待真机核验 | 小程序聊天 UI、5 个安全工具、云函数、多模型适配、本地 mock 降级；CloudBase preflight 通过，正式 provider 真机回答待确认 |
| 云端数据同步 | 🟠 部分实现 | AI 首次会话会把 7 类本地数据备份到按 openid 隔离的 cloudDB；“关于”页已披露；尚不是小程序全局双向同步，也缺少用户自助删除入口 |
| MCP Server | ✅ 演示可用 | 23 个工具、10 个资源、3 个提示词；TypeScript 构建通过，冒烟测试 46/46，集成测试 11/11 |
| Contract-first API | ✅ 演示可用 | 共享 Zod/oRPC 合约、Express OpenAPI Handler、summary-only trace、安全与 agentic 评测、家庭协作、本地持久化及菜单影响接口；34 项测试通过 |
| React Solution Console | ✅ 演示可用 | React 19 架构总览、安全实验室、可观测性与 agentic 评估、家庭协作控制台；18 项 MSW 测试及 2 项真实 Chromium E2E 通过，行覆盖率超过 98% |

当前内容资产：29 个食物分类、32 种基础食材、127 道食谱、9 条搭配禁忌。

## 3. 产品原则

### 3.1 安全优先

- 所有推荐、菜单生成和新食材尝试均必须经过确定性安全规则。
- AI 不得绕过食物安全判断，也不得直接执行“永久过敏”等不可逆操作。
- 疫苗后、肠胃不适、观察期和排敏窗口应影响菜单与下一步建议。
- 严重不适场景必须优先提示及时就医，不继续给出试吃建议。

### 3.2 少做选择题

- 默认给出一个清晰的下一步，而不是仅提供大量食谱供用户自行筛选。
- 需要替换时给出少量、可解释且符合库存与安全条件的候选项。

### 3.3 记录服务于下一次决策

- 饮食打卡应驱动库存扣减、偏好沉淀、首次食材识别和周复盘。
- 反应记录应驱动观察期、可疑食材分析与后续推荐限制。

### 3.4 本地优先、上传透明

- 非 AI 核心流程继续以微信本地存储为主。
- 任何发送到云端或模型服务的数据，都必须在用户可见的隐私说明中明确披露并取得必要同意。

## 4. 核心用户旅程

### 4.1 首次使用

1. 家长填写宝宝姓名、生日和每日辅食餐数。
2. 勾选宝宝已经吃过的食材，系统建立初始排敏档案。
3. 可选录入现有库存，完成后生成首份计划。
4. 进入“今日”页，看到今日菜单和最优先行动建议。

验收标准：完成流程后必须存在有效宝宝档案；生日必填；跳过冰箱不阻塞进入首页；已有食材状态可在档案页继续编辑。

### 4.2 每日喂养

1. 家长查看今日每餐与准备事项。
2. 可打开食谱、替换单餐，或根据系统建议调整明日计划。
3. 喂养后选择“按计划吃”或编辑实际菜名、食材、份量和备注。
4. 系统写入记录并扣减相应库存；撤销时恢复库存。

验收标准：已打卡餐不可因整周重排丢失；库存不得扣成负数；手动补记可不绑定食谱；撤销操作必须同时回滚记录与库存。

### 4.3 新食材排敏

1. 系统从未引入食材中给出下一步推荐。
2. 开始尝试前检查月龄、身体状态、当前尝试窗口和安全规则。
3. 进入连续尝试窗口，在首页展示当前天数和下一步动作。
4. 达到要求后确认通过，或因不适进入观察/过敏处置。

验收标准：同一时间只允许一个进行中的尝试窗口；未达到要求天数不可提前完成；疫苗后或活动性肠胃反应时不得启动；状态变更后剩余菜单必须清除不再安全的食材。

### 4.4 反应记录与回溯

1. 家长记录反应类型、严重度、发生时间和备注。
2. 系统读取此前 72 小时饮食记录，列出可疑食材及规则理由。
3. 家长选择观察、标记过敏或暂不处理。
4. 记录进入时间流，可编辑、标记消退或删除。

验收标准：分析结果必须可追溯到规则与历史记录；不得把已确认安全的食材默认列为高风险；永久过敏必须经过用户显式确认；严重症状必须展示就医提示。

### 4.5 AI 问答

1. 家长可询问今日菜单、食物安全、食谱、饮食记录或反应处理。
2. 云函数以 openid 隔离数据，模型通过受限工具读取和执行业务能力。
3. 页面展示回答，并允许查看工具调用轨迹；云能力不可用时进入本地 mock 演示模式。

验收标准：AI 不得推荐规则判定为不安全的食材；不可逆操作只能引导用户前往 UI 确认；无数据时明确说明；模型回答不得虚构宝宝档案或历史记录。

## 5. 功能需求

### 5.1 今日页（P0）

- 展示宝宝当前状态、日期、今日菜单与每餐完成状态。
- 支持食谱详情、单餐替换、两餐互换和更多候选。
- 支持按计划打卡、编辑实际饮食、手动补记和撤销。
- 展示排敏进度、反应提醒、明日预处理及下一步建议。
- 支持正常/疫苗后状态切换，并触发后续菜单调整。
- 支持分享小程序卡片与时间线。

### 5.2 计划页（P0）

- 按宝宝月龄、餐数、排敏状态和身体状态生成计划。
- 重新生成时保留已打卡餐次。
- 支持单餐替换并展示候选原因。
- 按选择天数汇总采购清单，并扣除已有库存。
- 支持标记“家里已有”、复制采购清单、复制/预览计划。

### 5.3 冰箱页（P0）

- 食材包含份数、存储位置、购买日期、到期日和处理状态。
- 支持添加、搜索、按位置/临期/低库存筛选和多种排序。
- 支持增减份数、调整到期日、移动位置、丢弃与批量操作。
- 支持加入采购清单、加入明日计划和查找相关食谱。
- 临期阈值默认为 2 天；低库存阈值默认为 1 份。

### 5.4 记录页（P0）

- 合并饮食与反应为按日期分组的时间流。
- 提供全部、反应、新食材、爱吃和近期未吃筛选。
- 支持按菜名、食材和备注搜索。
- 展示近 7 天爱吃次数、新食材数、反应数与营养缺口。
- 支持新增、编辑、删除记录及导出文本。

### 5.5 我的与档案（P0）

- 支持编辑姓名、生日、每日餐数。
- 展示当前排敏状态摘要。
- 管理品类状态、单食材确认、自定义食材和个体例外。
- 支持重置全部本地数据；执行前必须二次确认。

### 5.6 AI 搭子（P1）

- 支持 Anthropic、DeepSeek/OpenAI-compatible provider 和 mock 模式。
- 正式家长端仅开放可安全审计的工具：读取档案、检查安全、查食谱、生成菜单、记录反应。
- 工具调用循环最多 5 次，对话历史最多 20 轮。
- 回答默认使用中文、控制篇幅，并可展开查看工具轨迹。

### 5.7 React Solution Console（作品集，不进入家长端）

- 本模块是可单独启动的 React Web 应用，不属于微信小程序的页面、分包或 WebView，也不写入小程序 `wx.storage`。
- 提供“方案总览”，解释 Experience、AI Orchestration、Safety Boundary、Quality & Delivery 四层架构。
- 提供“安全规则实验室”，允许切换日常、疫苗后和个体过敏三类演示档案，并提交最多 10 种食材。
- 结果必须展示总体结论、逐食材 PASS/BLOCK、可解释原因、搭配提醒、档案快照和 `decisionSource`。
- 页面必须明确标注“仅展示软件决策边界，不构成医疗建议”。
- API 加载、连接失败和重试状态必须对用户可见；没有模型 API Key 时仍可运行完整安全检查。
- 提供“可观测性与评估”页面，展示执行次数、阻断率、平均延迟、trace ID、provider 状态与固定回归案例。
- Trace 只保存数量、档案状态和结果计数，不保存食材名、宝宝姓名或备注；页面必须标明 `summary-only` 和进程重启后清空。
- 评测只复用现有确定性规则，不产生线上 trace，不将测试结果包装为模型能力。
- Agentic 评测使用 9 个固定合成问题，复用小程序与云函数离线 mock 的共享路由策略，量化工具选择正确率、安全阻断召回率、grounding 代理和端到端成功率。
- Grounding 代理只验证是否在回答前选择正确的数据来源工具，不得描述为线上模型回答准确率；`provider: mock-policy` 必须始终可见。
- 提供“家庭协作与安全档案”页面，复现主照护人、共同照护人和只读家人共同使用同一宝宝安全档案的真实场景。
- 共同照护人可以根据已存在的反应记录提交永久过敏变更申请，但不能直接改变档案；只读家人不能提交申请。
- 主照护人必须核对关联反应并以字面量 `true` 显式确认；成功后更新合成档案状态、递增版本并写入变更记录。
- 申请与确认必须携带调用方读取时的档案版本；版本已变化时必须拒绝过期写入、保留档案和菜单，并记录 `profile-version-conflict` 审计。
- 页面必须用同一个菜单生成预览证明变更的实际影响：申请待确认时保留鳕鱼菜品，确认永久过敏后由确定性规则排除该菜品并选择不含鳕鱼的安全替代。
- 菜单结果必须携带档案版本、`decisionSource: deterministic-rules`、被排除食谱和规则理由；LLM 不参与安全过滤。
- 页面只使用 `synthetic-demo` 家庭数据，不引入真实宝宝信息；合成状态可在本地单节点服务重启后恢复。生产环境的家庭邀请、身份验证、云端数据库、多副本并发与灾备仍未实现。
- 是否将家庭协作能力产品化到微信小程序，需要单独进行用户验证和产品决策；在此之前不得把 Web 原型描述为家长端已交付功能。
- 桌面端和移动端均需可用，不得出现横向溢出或依赖微信运行时。

## 6. 数据与技术边界

### 6.1 当前存储

- 小程序主流程使用 `wx.storage`，单设备、单宝宝。
- 核心数据包括：宝宝档案、冰箱、手动采购清单、饮食日志、反应记录、自定义食材和周计划。
- AI 云函数首次调用时会把上述 7 类数据上传至 cloudDB，按 openid 隔离，后续 AI 会话读取云端副本。
- 当前没有完善的本地与云端双向同步，清缓存或换设备仍可能丢失/分叉数据。
- React 家庭协作原型不读取上述小程序数据；其家庭成员、档案版本、申请和审计记录均为 API 本地文件中的合成数据，采用临时文件加原子重命名写入，服务重启后可恢复。测试环境使用隔离的进程内存储。

### 6.2 规则与 AI 边界

- 食物安全、菜单适用性、排敏状态机和不可逆操作确认由规则层负责。
- LLM 负责意图理解、工具编排和自然语言解释，不进入安全判定路径。
- 家长端 AI 不提供永久过敏写入工具。

### 6.3 作品集技术架构

- 仓库使用 pnpm monorepo 管理 `packages/contracts`、`apps/api-server` 与 `apps/web-console`，现有微信小程序保持原目录和构建方式。
- `packages/contracts` 使用 Zod 定义输入输出并生成 oRPC contract，是 HTTP 服务端和 React 客户端的唯一边界类型来源。
- `apps/api-server` 使用 Express + oRPC OpenAPI Handler 暴露安全检查、家庭协作和菜单影响接口；实现层复用 `utils/safety.ts`，不复制安全规则。
- 家庭协作写入采用 `expectedProfileVersion` 乐观并发控制；版本冲突是可审计的业务拒绝，不会覆盖其他照护者刚完成的安全变更。
- `apps/web-console` 通过 oRPC `OpenAPILink` 消费共享 contract；TanStack Query 管理服务端请求状态，Zustand 只管理本地实验场景与输入。
- React 测试使用 Vitest、Testing Library 与 MSW 覆盖成功、拦截、空输入、连接失败和重试路径。
- Playwright 使用独立端口启动真实 API 与 Vite，覆盖个体过敏安全阻断，以及只读拒绝、共同照护人申请、主照护人显式确认、档案更新和菜单安全替换；测试状态与日常本地持久化数据隔离。
- GitHub Actions 配置覆盖 Node 20/22 的 workspace 构建和测试；远程 CI 未作为当前验收证据，本地 `npm run verify` 是现阶段权威质量门禁。

## 7. 非功能需求

### 7.1 安全与合规

- 所有页面持续展示“仅供参考、不构成医疗建议”的清晰入口。
- 应用内“关于”页必须准确描述 AI 功能涉及的 cloudDB 与第三方模型数据处理；当前 v1.0.4 页面已完成基础披露。
- 上线前仍需补齐正式隐私政策、用户授权时机、云端数据删除方式和模型供应商披露。

### 7.2 可靠性

- 核心规则单元/链路测试必须全部通过。
- TypeScript 构建必须无错误。
- API 边界覆盖率门槛不得低于语句/行/函数 90%、分支 85%；当前语句/函数/行 100%，分支 97.67%。
- React 控制台覆盖率门槛为语句/行/函数 85%、分支 80%，并必须覆盖请求失败与安全拦截路径。
- 共享 contract 发生变更时，API 和 React 构建必须在同一次 workspace 门禁中通过。
- 云函数在无模型密钥时可降级，但正式环境必须显式显示当前服务不可用，不能把 mock 回答伪装成真实 AI。

### 7.3 性能与可用性

- 本地页面首屏目标 < 1 秒（以常见真机为准）。
- AI 请求应显示发送中状态、防止重复提交，并在失败后提供重试。
- 关键操作必须提供成功/失败反馈；不可逆操作必须二次确认。

## 8. 上线指标

内测阶段先验证产品闭环，不以规模增长为首要目标。

- 建档完成率：进入欢迎页的用户中，完成档案并到达今日页的比例。
- 计划使用率：完成建档后 7 天内至少生成一次计划的用户比例。
- 打卡转化率：展示的计划餐次中被记录实际饮食的比例。
- 7 日记录留存：首次打卡后第 7 天仍有新增记录的用户比例。
- 建议采纳率：单餐替换、排敏建议或明日调整被执行的比例。
- 安全事件：不安全食材进入推荐结果的次数，目标必须为 0。

当前尚未接入分析 SDK，因此上述指标属于上线后的埋点需求；埋点接入必须同步更新隐私政策。

## 9. 发布门槛与待办

### P0：发布前必须完成

- [x] 修复 MCP 跨项目 TypeScript 编译边界与日期相关测试，使构建通过、冒烟测试恢复 46/46；集成测试为 11/11。
- [ ] 在真机核验 AI 云函数是否已按正式 provider 部署并能返回真实回答；页面必须明确区分 cloud、mock 与错误状态。
- [ ] 完成核心流程回归：建档→生成计划→打卡扣库存→记录反应→进入观察→菜单重新计算。
- [ ] 补齐正式隐私政策、AI 数据授权时机、云端数据删除入口或开发者处理流程。

### P1：内测增强

- [ ] 云端数据正式双向同步与冲突处理，支持换设备恢复。
- [ ] 完整的数据导出/备份与账号数据删除入口。
- [ ] 增加关键漏斗与安全事件埋点。
- [ ] 扩充真实家庭场景测试：早产、医生禁忌、多种过敏、连续漏打卡。
- [ ] 完善预处理工序合并与多人协作分享。

### P1：作品集增强

- [x] 增加第一阶段可观测性与评估面板，展示 trace ID、规则执行汇总、provider 状态、延迟和错误恢复；确定性路径无 token/模型成本，明确显示 `provider: none`。
- [x] 为 agentic workflow 建立 9 个固定合成问题的可重复离线评估集，量化工具选择正确率、安全阻断召回率、grounding 代理和端到端成功率；明确不等同于线上模型回答准确率。
- [x] 增加 Playwright 真实浏览器回归，打通 React、oRPC/HTTP、确定性安全规则与协作状态变更，不依赖 MSW 模拟接口。
- [x] 增加多人照护安全档案变更原型：家庭角色、反应依据、主照护人显式确认、档案版本和变更记录；只使用合成数据。
- [x] 明确 React 家庭协作页是独立 Web 作品集，不属于微信小程序，也不连接小程序真实用户数据。
- [x] 为合成家庭状态增加单节点本地持久化、原子写入、重启恢复和档案版本冲突保护。
- [ ] 将家庭成员邀请、身份验证和档案变更接入真实云端存储，并补齐多副本并发控制、备份和恢复。
- [ ] 基于真实用户访谈决定是否把家庭协作迁入微信小程序；若进入家长端，再设计小程序入口、成员邀请、权限、通知、同步和删除机制。
- [ ] 形成一份面试用 Architecture Decision Record 和 5–8 分钟演示脚本，明确业务价值、关键取舍、风险和扩展路径。

### P2：后续规划

- [ ] 多宝宝与多照护者协作。
- [ ] 经专业审核的内容版本与规则变更审计。
- [ ] 更完整的个性化营养趋势，但不替代专业诊疗。

## 10. 本次核验记录

2026-08-05 P0 本地持久化与版本冲突保护核验：

- 根目录 `npm run verify`：通过。
- 小程序 TypeScript 构建、聊天历史回归和备份回归：通过。
- Contract-first API：34/34 测试通过；语句 98.94%、分支 93.54%、函数 98.36%、行 98.92%。
- React Solution Console：生产构建通过，18/18 测试通过；语句 98.40%、分支 84.48%、函数 98.61%、行 98.34%。
- Playwright Chromium E2E：2/2 通过；真实 API/Vite 隔离启动，覆盖个体过敏阻断和家庭协作不可逆确认闭环。
- Agentic workflow 离线评估：9/9 通过；工具选择正确率、安全阻断召回率、grounding 代理和端到端成功率均为 100%；`provider: mock-policy`，未调用线上模型。
- Mock grounding 回归：菜单与档案回答必须来自工具结果，不再使用固定业务数据；安全工具返回阻断时必须引用规则原因并提示不要喂食，结果缺失或损坏时拒绝判断。
- 家庭协作真实 HTTP 验收：合成档案由 v1 确认至 v2，菜单确定性排除鳕鱼食谱并选择安全替代；API 服务重启后档案与两条审计记录恢复成功。
- React 与真实 API 联调：日常场景返回确定性结论与搭配提醒；个体过敏场景成功触发拦截。
- React 浏览器验收：此前桌面端与 390px 移动端完成检查，无横向溢出，浏览器控制台无应用错误；2026-08-05 本轮无可连接的应用内浏览器实例，未新增视觉验收证据。
- MCP Server `npm run build`：通过。
- MCP 单元测试：9/9 通过，当前纳入覆盖范围的语句/分支/函数/行均为 100%。
- MCP 集成测试：11/11 通过。
- MCP 冒烟测试：46/46 通过。
- 2026-07-27 已确认 `cloudfunctions/chat-ai/dryrun.js` 和 `scripts/deploy-chat-ai.sh --check` 通过，目标 CloudBase 环境为 `cloud1-d8g02cdnld86f3823`；本轮未执行云函数部署。
- 应用内版本号：`我的` 与 `关于` 已统一为 v1.0.4。
- “关于”页隐私说明：已披露不使用 AI 时本地缓存、使用 AI 时 7 类数据同步 cloudDB、问题与上下文可能发送给模型服务商、清理本地缓存不自动删除云端数据。
- 飞书正式 PRD：本次只更新本地 `PRD.md`，未同步外部文档。

## 11. 变更记录

| 日期 | 版本 | 说明 |
|---|---|---|
| 2026-08-05 | v0.17-draft | 增加 Playwright 真实 Chromium 回归，覆盖 React→oRPC/HTTP→确定性安全规则的个体过敏拦截，以及只读拒绝→照护人申请→主照护人显式确认→菜单替换闭环；纳入本地整仓质量门禁 |
| 2026-08-05 | v0.16-draft | 增加 9 个固定合成问题的 agentic workflow 离线评估，覆盖工具选择、安全阻断、grounding 代理和端到端成功率；小程序与云函数 mock 复用共享路由，并修复安全结果被固定回答覆盖的风险 |
| 2026-08-05 | v0.15-draft | 为家庭协作合成状态增加本地原子持久化和重启恢复；申请与确认使用档案版本拦截过期写入，冲突不改变档案或菜单并保留审计；明确单节点演示与生产云数据库边界 |
| 2026-07-31 | v0.14-draft | 明确家庭协作页是独立 React Web 作品集而非微信小程序页面；补充四类产品载体、合成内存数据边界及迁入家长端前的产品决策门槛 |
| 2026-07-30 | v0.13-draft | 打通家庭安全档案与菜单生成：待确认不改菜单，确认永久过敏后用共享确定性规则排除鳕鱼菜品、选择安全替代并展示规则证据 |
| 2026-07-30 | v0.12-draft | 将抽象企业治理演示重构为真实多人照护安全档案流程：共同照护人申请、反应依据、主照护人确认、档案版本更新与变更记录 |
| 2026-07-30 | v0.11-draft | 新增 mock IdP、确定性 RBAC、一次性显式确认、metadata-only 审计及权限控制台；明确模拟执行不会写入外部档案 |
| 2026-07-30 | v0.10-draft | 新增 summary-only 执行 trace、固定安全回归评测、React 可观测性控制台及成功/空状态/失败恢复测试 |
| 2026-07-28 | v0.9-draft | 新增 AI 工程作品集目标、Contract-first API 与 React Solution Console 范围；补充 monorepo 技术边界、前端/API 覆盖率门禁、真实联调证据和可观测性/评估增强路线 |
| 2026-07-27 | v0.8-draft | 修复 MCP 跨项目 TypeScript 编译边界和日期相关测试；确认小程序与 MCP 构建通过、冒烟测试 46/46、集成测试 11/11 |
| 2026-07-11 | v0.7-draft | 更新到 `/Users/x7/fushi-ditu` 当前状态；确认小程序构建、云函数 dryrun 和 CloudBase preflight 通过；修正内容规模、AI 隐私披露、版本统一与 MCP 测试状态 |
| 2026-07-10 | v0.6-draft | 按当前代码重建 PRD；补齐完整产品闭环、AI 架构、真实完成度、发布门槛与验收口径 |
